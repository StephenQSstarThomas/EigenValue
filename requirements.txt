# Dependencies for the CritPt reproduction & verification scripts (critpt_repro/repro/).
# Everything else used is in the Python 3 standard library (itertools, json, os, ...).
# Tested with: Python 3.13, numpy 2.4.4, scipy 1.15.3, mpmath 1.3.0.

numpy>=1.24
scipy>=1.10
mpmath>=1.3.0
